<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ใช้เน็ตแค่จุดเดียว คือตอนเรียก Gemini API ในหน้า "แนะนำโดย AI" -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GinRaiDee">

        <activity
            android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.ChoiceActivity"
            android:exported="false" />

        <activity
            android:name=".controller.RandomActivity"
            android:exported="false" />

        <activity
            android:name=".controller.ResultActivity"
            android:exported="false" />

        <activity
            android:name=".controller.AiActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

    </application>
</manifest>
